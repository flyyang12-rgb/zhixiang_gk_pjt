# 知向高考志愿推荐平台实施计划

## 一、产品目标

面向高考学生和家长，建设一个以“省份位次”为基础、融合城市偏好、性格特征、专业前景和院校数据的志愿辅助决策平台。

平台只提供决策参考，不承诺录取结果。所有推荐结果必须展示数据年份、来源和不确定性。

## 二、核心业务流程

1. 用户填写高考省份、选科、分数和全省位次。
2. 系统根据本省历年投档位次筛选可选院校范围。
3. 用户完成简化版霍兰德性格和城市偏好测评。
4. 系统先推荐适合发展的城市，再筛选城市内的院校。
5. 系统结合性格、学科能力和就业方向推荐专业。
6. 输出“冲、稳、保”三档志愿建议及推荐理由。
7. AI 志愿顾问针对学校、专业、城市和职业路径继续答疑。

## 三、功能模块

### 1. 全国院校数据库

- 学校基础信息：名称、地区、层次、类型、经纬度、官网。
- 专业信息：专业代码、学科门类、优势专业、学科评级。
- 招生信息：年份、省份、选科、批次、最低分、最低位次、计划人数。
- 数据来源记录：来源网址、发布日期、采集时间和校验状态。

### 2. 全国院校地图

- 按省份展示学校数量和学校层次。
- 支持 985、211、双一流、本科、专科筛选。
- 支持按学校、城市、专业搜索。
- 点击地图省份后展示当地学校、产业和生活成本摘要。

说明：“一本、二本”在新高考省份已逐步取消，数据库保留历史批次信息，前台主分类建议使用“本科、专科、985、211、双一流”。

### 3. 推荐引擎

- 第一层：按省份、选科和位次过滤不可达学校。
- 第二层：按城市偏好、距离、生活成本和产业机会评分。
- 第三层：按霍兰德类型、学科能力和职业倾向匹配专业。
- 第四层：根据近三年录取位次波动划分“冲、稳、保”。
- 推荐结果必须给出可解释原因，而不是只输出学校名单。

### 4. AI 志愿顾问

- 使用本地院校和招生数据库作为知识来源。
- 对话前自动读取用户测评和推荐结果。
- 回答附带数据年份和来源，缺少数据时明确说明。
- 可采用“直白、重视就业和家庭实际”的升学顾问沟通风格。
- 未经授权不得冒充张雪峰本人，也不得使用其姓名、肖像、声音或受保护内容制作数字分身。

## 四、技术方案

### 前端

- 第一阶段：HTML、CSS、JavaScript 完成交互原型。
- 第二阶段：迁移到 Vue 3 + TypeScript，拆分地图、测评、推荐、顾问组件。
- 地图建议：ECharts 中国地图或高德地图。

### 后端

- Node.js + NestJS，提供院校、专业、招生、测评和推荐 API。
- 推荐计算先使用可解释的规则引擎，数据稳定后再加入机器学习排序。
- AI 顾问通过后端访问大模型，密钥不得放在浏览器端。

### 本地数据库

- MySQL 8.0，字符集统一使用 `utf8mb4`。
- 开发阶段数据库名：`zhixiang`。
- 初始化脚本：`database/schema.sql`。
- 大批量院校数据使用 CSV 导入，原始文件和清洗结果分开保存。

## 五、实施阶段

### 阶段 1：数据底座

- 安装并初始化本地 MySQL 8。
- 完成省份、院校、专业、招生分数和数据来源表。
- 导入教育部全国高校名单，完成去重和地区标准化。
- 选取 3 个省份导入近三年投档数据作为验证样本。

验收标准：可按省份、层次、名称查询院校，可查询学校在指定省份的历年最低位次。

### 阶段 2：院校地图

- 接入中国地图。
- 实现省份、层次、学校名称和专业筛选。
- 完成学校详情抽屉和城市信息卡片。

验收标准：地图、列表和详情三者联动，筛选结果来自 MySQL API。

### 阶段 3：推荐系统

- 完成用户信息和霍兰德测评。
- 实现城市评分、院校可达性和专业匹配规则。
- 输出冲稳保方案和每项推荐理由。

验收标准：同一份输入结果稳定，所有推荐均可追溯到具体规则与数据。

### 阶段 4：AI 顾问

- 接入院校数据库检索。
- 注入用户画像和推荐结果。
- 增加事实校验、来源标注和敏感提示。

验收标准：顾问不编造招生数据，能解释推荐结果，并在数据不足时拒绝确定性结论。

### 阶段 5：测试与上线准备

- 完成接口、推荐规则和关键页面测试。
- 校验数据年份、异常值和缺失字段。
- 增加隐私说明、免责声明和数据更新机制。

## 六、近期执行顺序

1. 确认首批验证省份，建议先做河南、山东、河北。
2. 初始化本地 MySQL，并导入省份基础数据。
3. 建立院校 CSV 导入脚本。
4. 前端地图改为读取后端 API。
5. 用真实位次数据实现第一版冲稳保算法。
