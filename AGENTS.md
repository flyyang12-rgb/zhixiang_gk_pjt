# AGENTS.md

## Agent skills

### Issue tracker

任务以本地 Markdown 文件记录在 `.scratch/<feature-slug>/issues/`。详见 `docs/agents/issue-tracker.md`。

### Domain docs

本仓库使用单一上下文：根目录 `CONTEXT.md` 与 `docs/adr/`。详见 `docs/agents/domain.md`。

## 1. 适用范围与事实优先级

本文件适用于整个仓库，供开发、测试、数据导入和文档维护 Agent 使用。开始改动前，先阅读与任务直接相关的规格、实现和测试，不得只依据旧计划推测现状。

发生冲突时按以下优先级执行：

1. 用户在当前任务中的明确要求。
2. `docs/SPEC.md` 的当前 Solution、Implementation Decisions、Out of Scope 和验收口径。
3. `CONTEXT.md` 中仍在当前流程使用的领域术语。
4. 本文件的工程、数据、安全与交付约束。
5. `docs/REQUIREMENTS.md` 和 `README.md`。
6. `PLAN.md` 仅作早期历史背景，不是当前实现依据；其中 NestJS、测评、家庭偏好等旧方案不得用于恢复已删除流程。

若文档内部存在新旧描述冲突，以明确写明“两步主流程、不提供测评/答题/家庭偏好入口”的段落为准。需求或领域模型发生实质变化时，先更新 `docs/SPEC.md`，再修改实现和其余文档，避免代码、接口、测试和文档长期互相矛盾。

## 2. 当前产品基线

“知向”是面向高考学生与家长共同使用的本地志愿规划工具。核心价值是让家庭用可核验的招生和就业证据比较专业与学校，而不是用问卷或 AI 直接给结论。

当前不可回退的产品约束：

- 主流程固定为“基础信息 → 专业与学校”两步。
- 不提供双视角、霍兰德测评、性格答题、家庭偏好填写、测评结果页或独立“完善方案”步骤。
- 不把历史测评或家庭偏好计入完成度、专业排序、冲稳保或顾问上下文。
- 建档时选择“目标探索”或“志愿填报”，该选择作为原始阶段记录保留。没有规划位次时只作目标探索；后续只要历史模考形成规划位次，工作台自动开启学校冲稳保与专业交叉候选，不要求重建档案或手动切换模式。
- 工作台展示专业档位、就业方向、证据覆盖、院校/招生单元候选、收藏和顾问入口。证据不足时少展示或明确缺口，不凑数。
- 地图、工作台、推荐结果和收藏中的学校统一打开同一个右侧学校详情抽屉，不新增学校详情大页面。
- 收藏专业、排除专业和目标院校均跟随学生档案保存在 Supabase PostgreSQL；收藏后必须有明确反馈，并能从“我的收藏”重新进入学校详情。
- 产品采用无注册、全公开共享模式。所有访客都能查看、修改和永久删除全部学生档案、收藏、推荐结果及顾问聊天，不实现家庭间的账号或权限隔离。
- 学校详情和专业详情提供简短的确定性证据解读。AI 顾问只负责解释，入口只预填问题，不自动发送。
- 所有结论仅作分析与家庭讨论依据，不构成录取、就业、收入或升学承诺。

## 3. 当前技术栈与架构边界

- 前端：Vue 3、TypeScript、Vite、ECharts。
- 后端：Node.js、Express 5、TypeScript、Zod。
- 数据库：新加坡区域的 Supabase PostgreSQL，通过 `pg` 参数化访问。
- 测试：Vitest 与 Playwright。
- AI：OpenAI 兼容聊天接口，当前可配置 DeepSeek；异常时回退本地规则解释。
- PDF：服务端根据已保存档案和候选生成。

本地开发不使用 Docker；正式托管目标为 Vercel + Supabase，旧 Docker Compose/MySQL 部署路径已经退役。未经明确需求，不得把 Docker 重新引入开发或部署链路，也不得为“以后可能需要”引入 NestJS、ORM、大型 UI 框架或前端状态管理框架。

职责边界：

- Vue 组件负责展示、输入和交互状态，不承担 SQL、复杂推荐规则或数据来源判真。
- `server/` 负责路由、校验、规则编排和数据库访问；确定性规则优先提取为可测试纯函数。
- AI 无权生成招生事实、就业事实、专业分类、候选排序或冲稳保等级。
- 前端不得读取数据库配置、AI Key 或任何服务端私密信息。

## 4. 目录职责

- `src/`：Vue 页面、组件、样式、前端 API 类型与客户端、纯前端工具。
- `src/components/`：地图、专业工作台、学校详情抽屉、顾问和推荐展示。
- `server/`：Express 路由、专业工作台、招生候选、推荐、顾问、报告、就业数据和数据库访问。
- `database/schema.sql`：数据库结构、外键、唯一键和基础数据。
- `scripts/`：数据库初始化、官方数据导入、学校链接采集/核验、就业来源导入。
- `data/`：只读原始或标准化导入输入及经过审核的快照；不得存放用户隐私导出。
- `tests/`：Vitest 单元/规则测试和 Playwright 用户流程测试。
- `docs/`：规格、需求和架构决策。
- `.scratch/`、`logs/`、`test-results/`、`dist/`：临时或生成内容，不作为业务事实来源，也不提交为产品数据。

## 5. 本地运行与常用命令

首次安装：

1. 将 `.env.example` 复制为 `.env`，填写服务端 Supabase `DATABASE_URL`。
2. 双击 `setup-local.cmd` 安装依赖并检查环境文件。正式 Supabase 已初始化公共基础数据，不在日常本地启动时重复导入。
3. 以后双击 `start-local.cmd`，或运行 `npm run dev`。

默认地址：

- Web：`http://localhost:5173`
- API：`http://localhost:3000`
- 健康检查：`http://localhost:3000/api/health`

常用命令：

```powershell
npm run dev
npm run dev:web
npm run dev:api
npm run db:init
npm run data:schools
npm run data:school-links
npm run data:school-links:auto -- --concurrency 12
npm run data:featured-majors
npm run data:employment-sources -- data/employment-sources.json
npm run data:shandong
npm run data:henan
npm run data:henan-group-majors -- data/henan-group-majors.json
npm run data:hebei
npm run data:admissions -- data/admission-import.example.json
npm run data:admissions:rollback -- <batch-id>
npm run data:audit
npm test
npm run test:e2e
npm run build
```

若系统代理干扰本地请求，验证接口时显式绕过代理：

```powershell
curl.exe --noproxy "*" http://127.0.0.1:3000/api/health
```

## 6. 配置、隐私与密钥安全

本地配置只写入 `.env`，仓库只维护无真实值的 `.env.example`。

```dotenv
PORT=3000
DATABASE_URL=
DATABASE_SSL=true
DB_POOL_MAX=1
AI_BASE_URL=
AI_API_KEY=
AI_MODEL=deepseek-v4-flash
```

- `DATABASE_URL` 只允许存在服务端环境变量中；浏览器不得持有数据库密码、Supabase service role key 或直连数据库。
- 真实密码、API Key、Cookie、学生档案、位次和聊天内容不得进入代码、SQL 种子、测试快照、日志、文档或提交记录。
- 用户在对话中粘贴过的密钥视为已暴露，应提示轮换；输出中不得复述完整密钥。
- 错误响应和普通日志不得包含 SQL、连接串、密钥、服务端堆栈或其他档案数据。
- 档案和聊天保存在共享 Supabase 数据库中，不承诺私密或家庭隔离。所有访客都具备查看、修改和永久删除能力。
- 删除档案必须由访客明确触发并二次确认；测试只能清理本次创建且带明确测试标记的数据。

## 7. 数据事实、链接和导入

### 7.1 事实来源优先级

1. 教育部、各省教育考试院等主管部门官方公开数据。
2. 院校官网、本科招生网和院校发布的招生章程/专业目录。
3. 明确标注官方来源的公开镜像，仅在官方文件无法稳定获取时作为补充，并显式披露。
4. 搜索结果只能用于发现候选入口；营销网站、自媒体和 AI 生成内容不得进入正式事实或推荐。

每条参与推荐的招生数据必须可追溯到省份、年份、科类/选科组、批次或招生单元、来源 URL、发布方和采集时间。

### 7.2 招生数据边界

- 全国院校基础名单来自教育部公开名单。
- 可追溯投档推荐当前重点覆盖山东、河南、河北 2023—2025；并非三省所有年份、科类和专业都已完整覆盖。
- 河南改革前文理科与改革后物理/历史类不得直接平均或混算。
- 招生事实必须保留 `exact_major`、`major_group`、`school_line` 粒度。缺少官方组内专业目录时，不得从专业组名称推测具体专业。
- 数据缺失时降低置信度、展示缺口或停止自动推荐；不得使用 AI、模拟值或跨制度数据补齐。

### 7.3 学校官网与招生官网

- `official_url`、`admissions_url`、`links_verified_at`、`links_source_url` 是一组核验事实，不能只填网址不留核验依据。
- 页面只能把已核验且仍有效的链接展示为可点击官网；缺失或失效时显示“待核验”，不得猜域名、拼接子域名或用搜索页替代。
- `data/school-links.json` 是人工复核后的显式导入输入；自动采集只在 `.scratch/` 生成候选和报告，不得直接更新 `schools` 或进入用户详情。
- `npm run data:school-links:auto` 用于发现和初步核验缺失链接；候选仍需人工确认学校身份、官方来源和页面内容后才能复制到正式输入。发现或导入失败不得覆盖数据库中已有的有效核验结果。
- 优势专业只读取 `school_featured_major_evidence` 中带官方专业名称、认定类型、发布方、HTTPS 来源和核验时间的记录；有逐专业认定年份时展示认定年，综合名录只展示名录年份。旧 `school_majors.is_featured` 不能作为用户可见的已核验证据。
- `npm run data:featured-majors` 必须精确匹配学校名；官方专业代码或名称能匹配标准专业库时才写入 `major_id`。无法映射的官方专业仍可作为学校事实展示，但不得进入专业推荐关系，并须进入 `.scratch/featured-major-import-report.json` 的 unmapped 清单。
- 自动采集脚本必须设置并发上限、请求超时、重定向限制和异常报告；不得绕过验证码、登录、TLS 安全或站点访问限制。
- 链接覆盖率是动态数据，应通过数据库或导入报告计算，禁止在组件、文档或测试中硬编码总数冒充实时覆盖。

### 7.4 就业数据边界

- 招聘覆盖只代表最近 30 天公开岗位样本，不等于就业率、就业质量或收入保证。
- 来源默认禁用；只有条款允许公开读取、无需登录/验证码且结构稳定时才能启用。
- 不绕过 412、登录、验证码、反爬或访问控制。
- 健康来源少于 2 个、数据超过 7 天未更新或质量异常时，停止输出就业排名并明确降级。
- 专业—岗位方向映射由人工审核的数据决定，AI 不参与分类。

### 7.5 导入纪律

1. 原始文件只读保存，并记录来源、采集日期和文件校验信息。
2. 清洗为结构稳定的 CSV、JSON 或 XLSX 输入。
3. 导入前校验字段、范围、年份、来源、重复项、空值和招生粒度。
4. 使用业务唯一键或 upsert，保证脚本可重复执行。
5. 输出新增、更新、跳过、缺失和异常数量。
6. 抽样对照原始来源、标准化文件和数据库记录。

不得手工修改少量数据库记录来掩盖导入脚本问题；应修复导入或标准化逻辑的根因。

## 8. 历史兼容数据隔离

数据库仍保留 `profile_assessments`、`profile_preferences` 和相应兼容接口，用于不破坏既有本地档案；它们不是当前产品流程。

- 不新增测评、答题、家庭偏好、权重确认或测评结果 UI。
- 不从 `profile_assessments` 或 `profile_preferences` 读取数据影响专业评分、候选排序、冲稳保、完成度或工作台文案。
- 前端遗留的 `ProfilePreferences`、`getPreferences`、`savePreferences` 以及后端兼容路由只允许在明确的历史迁移/清理任务中使用。
- 删除历史表、字段或兼容接口属于数据库迁移，必须有备份、幂等迁移脚本和旧档案验证；不得顺手删除。
- 新代码不得继续扩大这些历史接口的使用范围。

## 9. 推荐与解读规则

### 9.1 模式与位次

- `exploration`：用于尚无规划位次的目标探索，只展示专业/就业证据、可核验学校和收藏，不显示冲稳保或录取预测；档案原始模式为 exploration 但已有规划位次时，实际工作台仍自动进入联合推荐状态。
- `application`：使用当前省份、科类/选科和可靠全省位次生成招生单元候选。
- 位次是录取比较主指标，分数只作辅助坐标；缺少可靠位次不得伪装成正式填报建议。
- 有多次有效模考全省位次时，自动推荐使用最近最多 5 次的中位数作为规划位次，并披露样本数、波动和稳定度。不平均裸分，不把趋势外推为高考成绩或录取概率。
- 专业工作台的学校候选、专业—学校交叉证据、家庭简报和顾问均以是否存在规划位次为准，不得再以档案原始 `planning_mode` 隐藏学校推荐。专业列表直接披露可核验学校数量并预览最多两所。

### 9.2 专业工作台

- 先执行选科等硬条件过滤，再按确定性规则排序。
- 当前专业评分因子为近期招聘覆盖 30%、本科直接就业入口 20%、可达院校 25%、近期需求稳定性 10%、未来发展证据 15%；至少两个独立因子有效且证据权重覆盖达到 50% 才展示综合参考分。改变权重属于产品规则变更，需同步规格和测试。
- 默认按“优先了解 / 值得比较 / 谨慎报考”各最多 3 个展示。档位是比较顺序，不是性格结论或“最适合专业”。
- 专业三句解读必须由现有评分因子、岗位方向和院校证据生成，包含进入当前档位的原因、就业/深造门槛和最需核验的风险。
- 无专业—院校证据时保留专业方向并标注未知，不把未知按 0 分处理，也不补造学校。

### 9.3 学校与冲稳保

- 候选必须区分省份、年份、科类/选科组、批次和招生粒度。
- 近三年不足时降低置信度并说明缺失年份；制度变化时只使用可比年份。
- 没有位次的数据可浏览，但不能参与自动冲稳保。
- 河南 2025 单年专业组规则继续使用已测试的单年位次比例，并统一标为低置信；不得与 2023—2024 文理科混算。
- 只有具体专业记录或已核验的专业组成员可以建立专业—学校交叉关系。学校线和未核验专业组只能作为学校/组层参考。
- 风险分档和解释由确定性规则生成；AI 不参与计算、排序或改档。

## 10. 学校详情与收藏交互

学校详情抽屉是所有学校入口的唯一内部详情容器，应包含：

- 学校名称、层次、城市、办学类型。
- 学校定位、当前档案招生位置、主要风险/数据缺口三条解读。
- 仅展示经核验的优势专业；无数据时明确写“暂无经核验数据”。
- 面向当前档案省份和科类的招生单元、年份、最低位次、选科要求、置信度与来源。
- 已核验学校官网、招生官网、收藏学校和问顾问操作。

交互要求：

- 学校卡片主体打开内部详情；官网和招生网使用明确的外部链接，二者不可混淆。
- 无档案仍可看基础信息；档案相关区域显示“建立档案后查看”。
- 支持遮罩、关闭按钮、Esc 关闭、焦点恢复；移动端使用全屏抽屉。
- 打开和关闭抽屉不得重置地图省份、筛选词、分页或页面滚动位置。
- 收藏成功弹窗说明保存到哪个公开学生档案；刷新后仍应存在。

## 11. AI 升学规划顾问

- 对外统一称“升学规划顾问”或“知向规划顾问”。
- 目标读者是高中毕业生和不熟悉志愿术语的父母。默认用短句、日常词和“先说人话，再讲依据”的顺序；不主动给用户贴“农村家庭”“信息差”或收入标签。“位次、专业组、招生单元”等术语首次出现时立即用括号解释。
- 顾问上下文只使用当前学生基础档案、专业工作台、收藏/排除项、候选清单和可追溯招生/就业证据，不读取历史测评或家庭偏好。
- 顾问可以解释、比较、追问和指出数据缺口；不得新增专业、创建招生事实、改变专业档位、候选排序或冲稳保。
- 从学校或专业详情进入顾问时，只预填具体问题，不自动发送，用户确认后才请求 AI。
- 顾问入口先建立未保存草稿，第一次发送才创建数据库会话；同一会话使用滚动摘要和最近 16 条消息理解指代，但每轮事实都重新从当前数据库读取。
- 每轮先判断用户真正在问什么，再选回答结构。问候、感谢、身份确认、记忆确认、情绪和“能不能报某专业”等单点问题用自然短答，不强套标题、不逐字复述前文、不先倒出档案和全局专业榜。复杂比较和风险分析可自然分段，但不得把四段标题当成固定模板。
- 学校、专业、就业、考研、报考判断、学费和宿舍等事实/决策问题使用“透明三句话”：“现在能确定”只能重述当前数据库能证明的事，“还不能确定”必须说清具体缺什么，“下一步只做”只给一个家庭立即能做的动作。问候、感谢、身份说明、记忆确认和单纯情绪回应不套该结构。
- “透明三句话”是信息边界，不是语气模板。正文仍要直接接住用户这句话，给出鲜明但有条件的倾向，然后用普通家庭听得懂的课程、位次、城市、就业门槛和成本解释。可以尖锐批评错误选择、招生宣传和侥幸心理，但始终“说选择，不骂人”，不攻击学生、父母、收入或出身。
- 从院校/专业详情入口创建的结构化焦点会话继续围绕原对象。普通无焦点会话中，用户可在任意一轮直接提到学校；服务端必须从本地 `schools` 中解析正式 ID 和名称。只有完整名或足够长且唯一命中的简称才能建立焦点；“理工、师大、科大”等过短或多义称呼不得猜测，应请用户说完整名。
- 普通会话首次成功识别院校后，必须持久化 `focus_type/focus_id/focus_name` 和会话标题，后续“它、这个学校、刚才那所”按该焦点理解，历史会话的返回目标也以当前持久化焦点为准。
- 顾问中的“搜索学校”默认指查询 Supabase 中的院校目录和已核验证据，只用于实体识别和当轮事实加载。运行时不联网补齐招生、就业、学费、宿舍或排名；数据库无证据时必须说出具体缺口。
- 学校焦点只限定事实读取范围，不得导致各轮重复同一学校介绍。用户表达目标专业时核对具体专业证据；用户指出重复或答非所问时先承认，再根据最近的用户目标重新回答。
- 用户问“某学校怎么样”时，必须进入院校整体评价路由，开头给明确倾向，再讲学校层次、地域成本、已核验优势专业和当前招生证据；不得回退到学生档案概述或全局专业清单。用户再问某专业时，只核对该校该专业的具体证据，不重复学校通稿。
- 院校评价和收藏院校比较在硬条件通过后以目标专业为首：先核对具体招生与已核验优势证据，再看规划位次、调剂与证据缺口、城市成本，最后才看学校层次与名气。允许明确站队、劝退并指出“不划算”或“在赌专业”，但不得新增院校、改排序、改冲稳保或把冲刺说成稳妥；默认用家长和高中生能一次看懂的 4—6 句短答。
- 运行时只读取仓库内锁定的安全改编方法论，不直接注入上游真人角色提示词，也不冒充张雪峰本人或授权服务。
- 从学校详情进入时，前端只提交学校 ID；服务端调用统一学校详情读取逻辑，向 AI 或本地降级解释提供对应学校的招生记录、风险、来源和缺口。非法学校 ID 必须明确失败且不得写入聊天历史。
- 涉及事实时标明年份和来源；无法核实时明确说“当前数据不足”或“不知道”。
- 模型回答使用纯文本，不输出 `**`、`##`、代码围栏等 Markdown 标记，不显示“综合参考分、低置信度、本科直接就业入口分”等内部术语。历史助手消息在前端展示时先做非破坏性纯文本清理；不改写数据库原消息。
- 事实链接只能由服务端根据当轮真实上下文生成 `evidenceRefs`，AI 无权自己编链接、发布方或年份。旧助手回答只用于语义记忆，不得当作新一轮的事实来源。
- 不得冒充张雪峰本人、官方服务或授权数字分身，不使用真人肖像/声音背书，不复制辱骂、歧视、制造焦虑或唯收入论表达。
- AI 未配置、超时、限流、结构无效、出现 Markdown、内部术语、无来源数字或越界承诺时自动回退本地规则解释。本地降级必须与 AI 使用同一意图路由、透明三句话边界和自然口语，不得回退到通用专业榜或生硬模板。

## 12. 当前 API 约定

实际主要接口：

- `GET /api/health`
- `GET|POST /api/profiles`
- `GET|DELETE /api/profiles/:id`
- `GET /api/schools`（分页与筛选）
- `GET /api/schools/:id?profileId=`（统一学校详情）
- `GET /api/map/provinces`
- `GET /api/admin/data-status`
- `GET /api/profiles/:id/profession-dashboard`
- `PUT /api/profiles/:id/saved-items`
- `DELETE /api/profiles/:id/saved-items/:itemType/:itemId`
- `POST /api/profiles/:id/recommendations/generate`
- `GET /api/profiles/:id/recommendations`
- `GET|POST /api/profiles/:id/advisor/messages`
- `GET|POST /api/profiles/:id/advisor/conversations`
- `GET|POST /api/profiles/:id/advisor/conversations/:conversationId/messages`
- `DELETE /api/profiles/:id/advisor/conversations/:conversationId`
- `GET /api/profiles/:id/report.pdf`
- `GET /api/employment/status`
- `POST /api/employment/sync-if-stale`
- `GET|PUT /api/admin/employment/sources`
- `POST /api/admin/employment/sync`

兼容接口 `GET|PUT /api/profiles/:id/preferences` 不属于当前用户流程。测评题目接口已删除，对其请求应返回 404。

统一响应结构：

```json
{
  "success": true,
  "data": {},
  "error": null,
  "requestId": "uuid"
}
```

- 所有外部输入必须通过 Zod 或等价显式校验。
- SQL 必须参数化，禁止拼接用户输入。
- 列表接口必须分页并限制最大返回量。
- 档案相关读取、删除、导出和 AI 请求必须验证档案存在。
- 404、422 和业务失败使用正确状态码，不得伪装成功；可降级场景在 `mode` 或中文提示中标识。
- 修改响应结构时同步后端、`src/api.ts` 类型、调用方和测试。

## 13. 前端与交互规范

- 保持简洁、克制、编辑式视觉。优先减少模块、文字和选择，不新增装饰性卡片、巨型标题或无意义动效。
- 首屏只呈现当前任务、关键证据和下一步；解释按需展开，不把所有数据同时堆在页面上。
- 主流程只有两步，完成度不得受历史兼容数据影响。
- 所有异步操作必须有加载、成功、空结果和失败状态；禁止静默失败。
- 动态数字、地图总数、地区数、收藏数和数据年份必须来自真实接口状态，不得硬编码掩盖数据问题。
- 缺少位次、官方来源、官网链接、就业来源或 AI 服务时，要同时说明缺什么、当前还能做什么。
- 用户可见文案使用中文；代码、数据库字段和 API 使用清晰英文命名。
- 顾问消息的“透明三句话”由前端从普通文本识别并突出展示，数据库和公开 API 不为此新增专用字段。无法完整识别三行时应按普通文本安全展示，不得丢失正文。
- 关键按钮、表单、弹窗、抽屉和地图需覆盖键盘操作、焦点可见、语义标签和常见移动端宽度。

## 14. 开发与文件操作约束

- 优先修复根因，避免只为截图效果增加模拟数据、条件分支或重复组件。
- 保持改动聚焦，尊重工作区已有修改；不要覆盖不属于当前任务的用户改动。
- 复用现有类型、API 客户端和纯函数；跨组件规则提取到独立模块并测试。
- 后端按路由、规则、数据访问和校验职责分离，避免继续堆积单文件逻辑。
- 不提交 `dist/`、日志、测试产物、数据库导出、真实用户数据或秘密配置。
- 不修改来源说明、置信度或核验状态来迎合预期结果。
- 外部请求必须设置超时、大小/重定向限制、异常处理和可解释降级。
- 不使用破坏性 Git 或文件操作清理用户工作区；仓库不是 Git 仓库时，明确说明无法提供基于提交的差异结论。

## 15. 测试与验证

先运行最贴近改动的验证，再扩大范围。没有新鲜测试结果，不得声称“已修复”“全部通过”或“数据已补全”。

最低验证矩阵：

- 纯函数、位次规则、映射和链接判定：新增/更新 Vitest，运行 `npm test`。
- Vue、TypeScript、服务端或 API 变更：运行 `npm run build`。
- 核心用户流程：运行相关 Playwright；准备完整交付时运行 `npm run test:e2e`。
- 两步流程：断言不存在双视角、测评、答题入口，旧测评 API 返回 404。
- 学校详情：覆盖成功、404、无档案、有档案、无招生数据、不同招生粒度、缺失官网和缺失优势专业。
- 抽屉流程：从地图、工作台、推荐结果和收藏打开同一详情，关闭后保留筛选/滚动位置，并验证 Esc、焦点恢复和移动端。
- 收藏：成功反馈、刷新持久化、从收藏进入详情、移除后状态一致。
- 顾问：学校/专业问题只预填不发送；正常、未配置、超时、异常和本地降级均不得编造事实。额外覆盖普通会话中途输入唯一院校简称、正式焦点持久化、“它/这个学校”追问、过短或多义称呼不猜测、院校评价不回退到通用专业榜、决策类三句话完整性、非决策类不强套结构和历史 Markdown 纯文本显示。
- 数据库结构：初始化可重复执行，外键和唯一键有效，删除只影响目标档案。
- 数据导入：重复导入幂等，统计与异常报告准确，抽样核对来源。
- 学校链接：验证同源/身份规则、危险 URL 拒绝、可信快照导入、失败不覆盖有效链接。
- 推荐：覆盖高分、压线、缺位次、目标探索、数据不足、改革前后隔离和三种招生粒度。
- 就业：覆盖健康来源不足、超过 7 天、同步失败和去重边界。
- PDF：能下载和打开、中文不乱码、内容与已保存状态一致，并包含来源、风险和免责声明。
- 地图：省份数量非零、名称映射正确、筛选/清除筛选、分页加载和接口真实总数一致。

若失败属于既有问题，不要顺带扩大修改范围；记录失败命令、现象、与本次改动的关系和未完成项。

## 16. 文档同步规则

以下变化必须在同一任务同步文档：

- 用户流程、模式、专业档位、冲稳保或 AI 权限变化：更新 `docs/SPEC.md`、`docs/REQUIREMENTS.md`、`README.md`。
- 领域术语变化：更新 `CONTEXT.md`。
- API、环境变量、启动或导入命令变化：更新 `README.md`、`.env.example` 和本文件。
- 数据来源、年份覆盖或已知缺口变化：更新规格中的数据边界和页面披露文案。
- 关键不可逆技术决策：在 `docs/adr/` 新增或更新 ADR。

不要把 `PLAN.md` 的早期设想当成已实现能力；需要继续保留时应在文件顶部明确其历史状态。

## 17. 交付检查

交付前确认：

1. 实现符合当前用户要求和 `docs/SPEC.md` 的两步主流程。
2. 没有恢复测评、答题或家庭偏好入口，也没有让历史数据重新影响结果。
3. 没有泄露密钥、隐私数据或内部错误信息。
4. 页面动态数字来自数据库或接口；数据年份、来源、粒度、缺口和风险说明完整。
5. 学校官网、招生官网和优势专业只展示经核验事实，缺失时明确待核验。
6. AI 不生产规则结果、不冒充真人，异常时可降级。
7. 收藏、学校抽屉、地图筛选和顾问预填在刷新/关闭场景下行为一致。
8. 已执行与改动匹配的测试和构建，并如实报告命令和结果。
9. 文档、`.env.example`、启动脚本、package scripts 和实际接口保持一致。
10. 顾问先回答当前问题，没有因“透明三句话”回退成模板；中途提到的唯一院校能从本地库识别、持久化并支持指代追问，歧义称呼不猜测。

## 18. 当前已知债务与改进顺序

后续工作优先按“事实完整性 → 决策可用性 → 交互收敛 → 工程优化”处理：

1. 继续提高院校官网、招生官网和优势专业的官方核验覆盖，但绝不为覆盖率猜测数据。
2. 扩充三省可比制度下的近三年招生记录和河南专业组官方成员映射。
3. 在现有 2—4 校比较基础上增加可选的家庭讨论备注和手动排序，不新增独立大页面。
4. 对齐尚存的历史兼容类型和接口；清理必须走显式迁移，不直接删表。
5. 继续细分地图大包，并持续补齐移动端、键盘和无障碍测试；不得用提高警告阈值代替优化。

## 19. 相关文件

- `docs/SPEC.md`：当前实现工作的权威需求来源。
- `docs/REQUIREMENTS.md`：完整产品需求与验收场景，含历史兼容说明。
- `CONTEXT.md`：领域通用语言。
- `docs/adr/`：关键架构决策。
- `database/schema.sql`：数据库初始化结构。
- `README.md`：面向使用者的安装、运行和数据维护说明。
- `.env.example`：不含真实秘密的配置模板。
- `docs/adr/0010-supabase-postgres-public-hosting.md`：Supabase PostgreSQL 与公开共享访问模型的架构决策。
- `PLAN.md`：早期历史计划，不作为当前实现依据。
